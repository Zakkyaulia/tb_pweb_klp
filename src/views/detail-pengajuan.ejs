<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detail Pengajuan - SIPAS</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-green-700 font-sans">
  <%- include('partials/header', { active: 'Riwayat' }) %>
  
  <main class="max-w-4xl mx-auto mt-10 p-8 bg-white rounded-xl shadow mb-10">
    <h2 class="text-xl font-bold text-white bg-orange-400 px-4 py-2 rounded-t-xl mb-6">
      <i class="fas fa-eye mr-2"></i>Detail Pengajuan Surat
    </h2>
    
    <div class="space-y-4 text-gray-800">
      <div class="flex">
        <div class="w-48 font-medium">Nama Mahasiswa</div>
        <div>: <%= data.nama %></div>
      </div>
      <div class="flex">
        <div class="w-48 font-medium">NIM</div>
        <div>: <%= data.nim %></div>
      </div>
      <div class="flex">
        <div class="w-48 font-medium">Jurusan</div>
        <div>: <%= data.jurusan %></div>
      </div>
      <div class="flex">
        <div class="w-48 font-medium">Tanggal Pengajuan</div>
        <div>: <%= new Date(data.tanggal_request).toLocaleDateString('id-ID') %></div>
      </div>
      <div class="flex">
        <div class="w-48 font-medium">Jenis Surat</div>
        <div>: <%= data.nama_lengkap_surat %></div>
      </div>
      <div class="flex">
        <div class="w-48 font-medium">Status Surat</div>
        <div>: 
          <span class="px-2 py-1 rounded-full text-xs font-semibold
            <% if (data.status === 'diproses') { %>bg-yellow-100 text-yellow-800<% } %>
            <% if (data.status === 'selesai') { %>bg-green-100 text-green-800<% } %>
            <% if (data.status === 'diajukan') { %>bg-blue-100 text-blue-800<% } %>">
            <%= data.status === 'selesai' ? 'Selesai' : (data.status === 'diajukan' ? 'Diajukan' : 'Diproses') %>
          </span>
        </div>
      </div>
      <div class="flex items-center">
        <div class="w-48 font-medium">Dokumen Pengantar</div>
        <div>: <%= data.file_pengantar || 'Tidak ada file' %></div>
      </div>
      <div class="flex items-start">
        <div class="w-48 font-medium">Catatan Admin</div>
        <div class="w-full">
          <textarea disabled class="w-full border-2 border-orange-400 rounded p-3 text-gray-500 resize-none mt-1" rows="4"><%= data.komentar_admin || 'Belum ada catatan dari admin' %></textarea>
        </div>
      </div>
    </div>

    <div class="flex justify-end px-8 py-6">
      <a href="/riwayat" class="bg-orange-500 hover:bg-orange-600 text-white font-bold py-2 px-4 rounded-lg transition">
        <i class="fas fa-arrow-left mr-2"></i>Kembali
      </a>
    </div>
  </main>
</body>
</html> 